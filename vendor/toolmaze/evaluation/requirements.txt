# ToolMaze Dependencies (kept here so `pip install -r evaluation/requirements.txt`
# from older docs still works). Prefer the top-level `ToolMaze/requirements.txt`.

openai>=1.0.0
anthropic>=0.18.0
pyyaml>=6.0
